# Sawmill Calculator

A React-based web application for calculating sawmill measurements, with a one-time payment modal using ClientJS for device fingerprinting.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
